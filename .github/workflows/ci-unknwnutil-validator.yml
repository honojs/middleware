name: ci-unknownutil-validator
on:
  push:
    branches: [main]
    paths:
      - 'packages/unknwnutil-validator/**'
  pull_request:
    branches: ['*']
    paths:
      - 'packages/unknwnutil-validator/**'

jobs:
  ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./packages/unwnutil-validator
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 18.x
      - run: yarn install --frozen-lockfile
      - run: yarn build
      - run: yarn test
