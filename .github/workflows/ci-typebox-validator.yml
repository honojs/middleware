name: ci-typebox-validator
on:
  push:
    branches: [main]
    paths:
      - 'packages/typebox-validator/**'
  pull_request:
    branches: ['*']
    paths:
      - 'packages/typebox-validator/**'

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun i
      - run: bun run --filter @hono/typebox-validator build
      - run: bun run --filter @hono/typebox-validator publint
      - run: bun eslint packages/typebox-validator
      - run: bun run test --coverage --project @hono/typebox-validator
      - uses: codecov/codecov-action@v5
        with:
          fail_ci_if_error: true
          directory: ./coverage
          flags: typebox-validator
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
